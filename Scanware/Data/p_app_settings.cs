using System;
using System.Collections.Generic;
using System.Linq;
using Scanware.Data;

namespace Scanware.Data
{
    public partial class application_settings
    {
        

        //public static application_settings GetAppSetting(string setting)
        //{
        //    sdipdbEntities db = ContextHelper.SDIPDBContext;

        //    //var rsp = db.Database.SqlQuery<application_settings>
        //      //           ("SELECT app_name, default_value, setting_name FROM application_settings").FirstOrDefault(x => x.setting_name == "Ship_From_Scanware");

        //    var rsp = db.Database.SqlQuery<application_settings>
        //                 ("SELECT app_name, default_value, setting_name FROM application_settings").FirstOrDefault(x => x.setting_name == setting);

        //    return rsp;

        //}
    }
}

